export function showPetByIdDataProvider(data: any): any {
  // mock api, you need to write code to call real backend service to get response data
  return {
    "id": 1,
    "name": "value",
    "tag": "value"
  };
}